# Pinned to what is installed and verified on this machine (macOS arm64, Python 3.13).
mujoco==3.13.0
mink==1.3.0
qpsolvers[daqp]==4.13.0
numpy==2.5.3
robot_descriptions==3.1.0
loop-rate-limiters==1.2.0
openai==3.13.0
rich==15.0.0
python-dotenv==1.2.3
pytest==9.1.1

# Voice (optional; --voice only). Apple silicon. Whisper runs locally, so speech
# to text costs nothing and works with no network.
mlx-whisper==0.4.3
sounddevice==0.5.6

# Chess scenario (optional; robot_agent/sim/chess_scene.py, not wired into the app yet).
# Legal-move checks only; the scene builds and renders without it.
chess==1.11.2
matplotlib>=3.8  # architecture diagram (python -m robot_agent.architecture)
fastapi>=0.115  # web UI (--web)
uvicorn[standard]>=0.30  # web UI (--web)
