# OpenAI-compatible endpoint. Works with the event's OpenAI credits, OpenRouter,
# or any local server. Never commit the real .env.
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-replace-me
OPENAI_MODEL=gpt-4o-mini

# Run fully offline with the mock planner instead: mjpython -m app.main --mock-llm
